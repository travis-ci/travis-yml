language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install pytest
  - pip install codecov
  - pip install pytest-cov
# command to run tests
script: 
  - sudo apt-get install -y unrar
  - pytest src --cov=src/kep
after_success:
  - codecov