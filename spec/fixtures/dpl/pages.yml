provider: pages
opts:
  github-token: str
  repo: current one
  target-branch: gh-pages
  keep-history: true
  allow-empty-commit: true
  committer-from-gh: true
  verbose: true
  local-dir: current
  fqdn: str
  project-name: str
  email: str
  name: Deployment Bot
  deployment-file: true
