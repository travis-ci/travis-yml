provider: gcs
opts:
  access-key-id: str
  secret-access-key: str
  bucket: str
  upload-dir: root directory
  local-dir: project root
  dot_match: true
  detect-encoding: not set
  cache_control: not set
